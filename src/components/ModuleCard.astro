---
const { index, id, title, description, items } = Astro.props;
---

<article class="module-card" id={id}>
  <span class="module-index">{index}</span>
  <h2>{title}</h2>
  <p>{description}</p>
  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</article>
